# 第一阶段真实源字段与能力边界

本说明先于后续派生实现更新。依据是原始 `data/README.md`、本地/服务器全量文件清单与 SHA-256 校验、业务目录首个真实 Parquet schema，以及关键表首尾分区和停牌事实字段检查。原始文件不修改、不覆盖、不删除；所有修正记录在项目配置和本说明。

2026-09 改进轮补充：本地原始数据新增 `namechange/`、`stk_limit/` 两个业务目录，`trade_cal/` 追加 20180101–20180531 两个分文件；本说明相应更新，派生约定见 `STAGE1_NEXT_IMPROVEMENTS.md`。

## 实际覆盖

原始基线为 69 个业务目录（109,320 文件、7,998,006,450 字节，本地与服务器逐文件 SHA-256 一致）。本轮新增 `namechange/`（5,907 文件）与 `stk_limit/`（2,116 文件），`trade_cal/` 增至 4 个文件；这确认本地与服务器基线内容一致，不代表供应商数据不存在缺失或延迟，新增目录的量级以本轮画像为准。

| 原始表 | 本次实测范围或规模 | 使用边界 |
| --- | --- | --- |
| `trade_cal` | SSE、CFFEX 各两个文件；合计 20180101–20260911，3,176 自然日、2,111 交易日 | A 股统一选择 SSE；两交易所文件日期范围当前一致。日历自 2018-01-02 起完整，更早历史仍无来源，不能编造未来下一交易日。 |
| `stock_basic` | 5901 行：L=5562、D=339；上市日期 19901201–20260911，已有退市日期 19990712–20260730 | 当前快照；保留退市证券并按上市/退市日期判定历史身份。当前名称、ST、市场板块等不等同于历史区间。 |
| `namechange` | 5,907 个按代码文件、14,205 行；区间双端包含 `[start_date,end_date]`，`end_date` 空表示至今；`(ts_code,start_date)` 无重复 | 历史名称与 ST 状态的事实来源（名称含 `ST` 字样即 ST）。7 组重叠区间 ST 语义一致；243 处区间内空洞、73 只股票首区间晚于 `list_date`、`T600018.SH` 无记录、7 个代码不在当前 `stock_basic`，均保留并披露，不猜测回填。 |
| `stk_factor_pro` | 20180601–20260911，2012 个日分区 | 原始 OHLC、`adj_factor` 及厂商 qfq/hfq 字段都存在；没有 `pre_close`。行情源起点晚于日历起点，`daily_adjusted_price` 覆盖门禁按源实际范围执行。 |
| `stk_limit` | 20180102–20260918，2,116 个日分区、9,866,957 行；`(trade_date,ts_code)` 无重复；SSE 日历 20180102–20260911 每个交易日均有分区 | 逐股逐日 `up_limit/down_limit`。280 行 BJ 占位值（99999.99/0.0）无效：保留原始行、排除出方向事实并诊断披露；155 个代码不在当前 `stock_basic`（含 B 股 200xxx）；约 6.2 万个有报价但缺限价行的证券日集中在 2018–2022。 |
| `suspend_d` | 20180601–20260911，2012 个日分区；S=80103、R=4489 | 999 条记录有非空 `suspend_timing`，其余可含全天停牌或复牌事实；不能把 R 当停牌。日历扩展后 20180102–20180531 无停牌覆盖，该区间未观测停牌保持未知。 |
| `limit_list_d` | 20200102–20260911，1624 个日分区 | 存在 U/D/Z 事件，不含 ST 股票统计。自本轮起降级为独立观测事件：方向判断优先使用 `stk_limit` 价格证据；与价格证据交叉验证 U 事件零矛盾。 |
| `income_vip`、`balancesheet_vip`、`cashflow_vip` | 各 36 个报告期分区，20170930–20260630；20170930/20171231/20180331 与 20260331/20260630 的补充行来自非 VIP 逐股接口（账号无 VIP 权限），只含 report_type=1 | 20180331 与尾部缺失股票已补齐；20170930 原始公告（2017-10）早于日历起点 20180101 而不可见；同键 update_flag=0/1 版本对累计指标值预检一致（仅 20260630 各 1 组冲突按 ambiguous 处理）。 |
| `fina_indicator` | 按股票 5849 个文件；首样本 000001.SZ 的报告期为 20180630–20260630 | 只有 `ann_date/end_date/update_flag`，没有 `f_ann_date` 或公告时刻。该范围是样本观察，不伪称全部股票范围。 |

其他业务目录均已登记进 `configs/tushare_tables.yaml`，路径、日期字段、代码字段与必需字段已核对真实首文件 schema。未用于第一阶段派生的表保留 `primary_key: []`，表示尚未确认主键，不能把它解释为不需要键检查。

原始 README 已登记 `index_daily/`，本项目将其列为仅画像的源目录。README 称 `index_weight` 为五大指数，实际已有 6 个指数文件；README 给出的 `dc_concept`/`dc_concept_cons` 接口历史起点为 20260203，但本地首分区实际是 20260417。接口支持起点和本地覆盖分别记录。月度 `month/MONTH`、季度或合约月份字段不假装成 `YYYYMMDD` 日字段；时间戳保留原义。

## 公告日期与财务版本

三张 VIP 财务均实际提供 `ann_date`、`f_ann_date`、`end_date`、`report_type`、`update_flag`。`f_ann_date` 是实际公告日；供应商的报告类型区分累计合并、单季及不同调整口径，不能混在一起做差分。[TuShare 利润表字段说明](https://tushare.pro/document/2?doc_id=33)

这些字段存在真实差异：`income_vip/by_period/20180630.parquet` 中有 71 行 `ann_date != f_ann_date`，该分区 `ann_date` 最晚为 20200327，`f_ann_date` 最晚为 20250425。只使用较早的 `ann_date` 会让后来公告的记录提前可见。因此 V1 明确采用：

1. 保留两个原始日期；公告依据日为非空有效日期 `max(ann_date, f_ann_date)`。
2. 从依据日之后的下一个 SSE 交易日开始可用，公告当天不能使用。
3. `fina_indicator` 没有 `f_ann_date`，按其 `ann_date` 滞后并标注源能力；没有日期的记录不能通过报告期补一个公告日。
4. `financial_available` 保留实际源中所有报告类型、冲突和版本；单季度/TTM 仅处理配置的累计 `report_type='1'`，避免把单季口径再差分。
5. 日期超出已知交易日历或缺少后续交易日时，明确报告不可对齐，不能强行映射到最后一个交易日或凭空延展日历。

配置中的财务键是候选键，不保证真实源唯一。相同股票、报告期、公告日期的多记录或不同金额需要在画像中报告并保留可追溯标识，不能静默覆盖。当前源文件不证明供应商保存了每次修订前的完整历史快照；V1 只能对现有版本执行公告滞后。

Step 3 全量画像进一步发现，相同候选键对应不同完整记录的冲突组分别为 `balancesheet_vip=31`、`cashflow_vip=2`、`income_vip=6`。例如利润表 `002961.SZ/20250630` 的冲突记录连 `update_flag` 都相同，因此不能把该标志当作任意择一依据。`financial_available` 以完整记录哈希标识并保留源版本；同一股票、同一生效日、同一报告期和指标出现相互冲突的金额时，受影响的单季/TTM 为 `NULL`，明确标记 `ambiguous`，不按输入顺序或 `update_flag` 静默选值。

`fina_indicator` 实测有 9 条 `ann_date` 是字面字符串 `None`。这些原始记录和原始值保留，另标 `invalid_announcement`，不成为研究日可见的财务记录；不得用报告期、文件名或当前日期补造公告日。`revenue` 等金额缺失继续保持空值，不能用 0 伪装成已披露的零金额。

累计白名单为利润表 `revenue/n_income_attr_p` 与现金流量表 `n_cashflow_act`。资产负债表的 `total_assets` 是时点余额，财务指标 `eps` 是每股指标，不允许机械差分或四季求和。缺 Q2 时 Q3 不能减 Q1，缺来源季度时 TTM 留空并说明原因；首期 20180630 缺少此前 Q1，同样适用。

## 历史代码和跨市场身份

全量核验显示，**行情中的全部 `ts_code` 都能匹配当前 `stock_basic`，行情 orphan 数为 0**。但财务源中有大量不能匹配当前基础表的历史代码，尤其旧北交所代码；当前数据不足以构造带生效日期、可核验的历史换码映射。财务记录必须保留原始 `ts_code`，不能以内连接当前基础表的方式丢掉，也不能猜测其新代码。跨代码财务关联能力暂挂。

当前 `stock_basic` 中另有 97 个证券没有任何行情：96 个 `list_status=D`，以及 `list_date=20260911` 的 `688801.SH`。缺行情不等于证券不存在，基础身份仍保留，行情可用性单独表达。

北交所证券还存在早于当前 `stock_basic.list_date` 的历史报价。例如 `920017.BJ` 的 `list_date=20230531`，但 20180628–20220609 已有 679 行报价。这些数据可能反映跨市场历史，现有来源没有足够身份区间证明，不能把较早报价当成已在 A 股上市，也不能把当前北交所身份回溯到全部历史报价。处理层保留全部原始代码和价格；明确上市身份以 `stock_basic.list_date` 为起点，跨市场历史覆盖不完整的部分单独标注、暂挂。此规则不要求删除上市前价格。

## 价格、停牌与涨跌停

V1 保留原始价格，用当日原始 OHLC 乘当日 `adj_factor` 得到研究价格；不使用未来某个末日因子归一，也不覆盖原始成交价格。厂商 qfq/hfq 字段可追溯保留，不用来猜测缺失历史。`ret_1d` 只在相邻交易日均有该股行情时计算，缺行情后不能跨多个交易日计算所谓一日收益率。

`suspend_type=S` 是停牌，`R` 是复牌；`suspend_timing` 非空表示日内时段，空值表示全天情形。比如原始 20180608 的 `601138.SH` 为 `09:30-10:00`，20180612 的 `300134.SZ` 为 `13:00-15:00`。V1 可以对出现日内停牌的股票采取保守买卖限制，但必须明确原因是“日内停牌的保守规则”，不能把事实描述为全天没有成交机会。[TuShare 停复牌字段说明](https://tushare.pro/document/2?doc_id=214)

`suspension_events_complete=true` 同时限定在配置的 20180601–20260911 当前源覆盖范围和 `suspension_complete_code_suffixes=[SH,SZ]`；BJ 因历史换码缺口保留未知。依据是对应交易日日分区齐全且 SH/SZ 停复牌事件均能匹配代码；它不保证未来追加范围、供应商外部真实性或盘中准确时序。缺行情必须单独报告，不能自动变成停牌事实。

`limit_list_d.limit` 的 U、D、Z 分别表示涨停、跌停、炸板；Z 不能当作收盘涨停。该接口自 2020 年起提供，官方明确不提供 ST 股票统计，因此 `limit_events_complete=false`。自 2026-09 改进轮起，方向判断优先使用 `stk_limit` 价格证据（`close >= up_limit` / `close <= down_limit`），U/D/Z 保留为独立观测事件；交叉验证 U 事件与价格证据零矛盾，价格证据额外覆盖官方不统计的 ST 股票。[TuShare 涨跌停列表说明](https://tushare.pro/document/2?doc_id=298)

`stk_limit.up_limit/down_limit` 是逐股逐日股票限价（20180102–20260918，2116 个日分区覆盖 SSE 日历全部交易日）。280 行 BJ 占位值（99999.99/0.0）按无效处理：原始行保留、不参与方向判断并在诊断披露；有报价但缺限价行的约 6.2 万个证券日（集中在 2018–2022）方向保持未知。`ft_limit.up_limit/down_limit` 仍属于中金所期货合约，不是股票日限价；除 stk_limit 外不按固定百分比补造限价。

## 暂挂能力及可独立工作

| 暂挂能力 | 缺少的真实源 | 仍可独立完成的内容 |
| --- | --- | --- |
| 历史退市整理期 | 整理期起止日期 | 用真实 `delist_date` 区分退市前后，不能倒推整理期；`is_delisting_period` 保持 NULL。 |
| 早期上市股票的精确上市交易日数 | 20180102 之前完整 SSE 日历 | 覆盖范围内新股可精确计数；老股给出 `listing_natural_days` 与 `estimated_listing_trade_days` 明确口径替代，精确值保持 NULL。 |
| 缺失原始季度的单季/TTM | 首期之前和部分中间期财报、尾部全市场财报 | 对齐已有公告版本，只对齐备来源计算；缺失与受影响输出留空。 |
| 公告时刻及完整供应商修订档案 | 盘中时间与每次修订前快照 | 执行公告日期后下一交易日滞后，并保留实际提供的版本。 |
| 历史证券代码映射 | 带生效日期且可核验的历史换码关系（财务、namechange 的 7 个与 stk_limit 的 155 个未匹配代码） | 保留所有原始代码和记录，暂不跨代码拼接；行情代码均能匹配当前基础表。 |
| 历史跨市场身份 | 北交所等证券跨市场迁移的明确生效区间 | 保留早期报价，上市身份按明确的 `list_date` 起算，不把当前市场身份回溯。 |

已解除的挂起（2026-09 改进轮依据新增源实现）：

- **历史 ST/\*ST 与完整更名**（active）：`namechange` 提供带区间的历史名称，`is_st/is_star_st/historical_name` 按交易日派生；名称含 `ST` 字样即 ST，`*ST` 同时为 `is_star_st`。区间空洞与重叠语义冲突保持未知。三个 `ex_st` 样本空间已生成成员。
- **股票每日双向限价**（active）：`stk_limit` 自 20180102 起提供逐股逐日限价，`daily_trade_status` 保留 `up_limit/down_limit` 事实并优先按收盘价比对判定涨跌停方向；缺失或占位限价的证券日保持未知，不将未知计为已证实可交易。

本次补充是全量画像后更新的处理约定和来源说明；已运行画像的配置 provenance 快照保留原样，后续构建记录其实际使用的新配置，不能改写先前画像的追溯信息。

人工 `tests/fixtures/golden/` 夹具（含 `namechange.csv`、`stk_limit.csv`）只用来验证规则，不能冒充真实数据源或解除仍挂起的能力。本阶段不实现因子、股票池、回测或交易信号。

Step 6 进一步确认 `stock_basic` 包含历史代码 `T600018.SH`（上港集箱退市标记），
上市日 20000719、退市日 20061020，`market` 为空、`curr_type=CNY`。该证券在新增的
namechange 中无记录，ST 状态保持未知。代码必须按供应商原样保留，不能强制六位数字而删除该历史证券。
当前 5901 条基础记录货币均为 CNY，已将 `curr_type` 登记为状态处理所需字段。
历史板块不以当前 `market` 回填，缺少历史区间时 `board` 留空。
## Step 7 全量关联核验补充

停复牌源有 41,318 条事件无法匹配当前证券日网格，全部为 BJ 旧代码
（S 40,998 条、R 320 条）；SH/SZ 无未匹配事件。配置新增
`suspension_complete_code_suffixes: [SH, SZ]`：仅该范围无事件可解释为未停牌。
BJ 已匹配正向事件仍有效，无匹配事件保留未知，历史换码功能继续挂起。

## 财务版本与查询约定

`financial_available` 保留四张财务表全部原字段与原始日期字符串，日期另标准化为 DATE。
完整原记录哈希为 `record_id`，重复内容合并时保留 `source_count/source_locations`。
`version` 只是同报告期的确定性序号，不是冲突记录的优先级。
`effective_ann_date=max(ann_date,f_ann_date)`；非空非法日期、无公告日、无效报告期、
公告早于报告期、公告超出日历或无后续交易日的记录不可见，并保留原因。
`financial_asof` 限制研究日处于日历覆盖，并只返回已到可用日的全部版本。
财务构建始终保留完整历史，不以行情区间的 start/end 截去差分或 TTM 所需前史。
